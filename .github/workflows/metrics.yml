name: GitHub Metrics

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  github-metrics:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Generate GitHub Metrics
        uses: lowlighter/metrics@latest
        with:
          # =========================================================
          # BASIC CONFIGURATION
          # =========================================================
          token: ${{ secrets.METRICS_TOKEN }}
          user: soo6ya

          template: classic

          base: header, activity, community, repositories, metadata

          config_timezone: Asia/Kolkata

          # =========================================================
          # CONTRIBUTION CALENDAR
          # =========================================================
          plugin_isocalendar: yes
          plugin_isocalendar_duration: full-year

          # =========================================================
          # PROGRAMMING LANGUAGES
          # =========================================================
          plugin_languages: yes
          plugin_languages_sections: most-used
          plugin_languages_limit: 8
          plugin_languages_threshold: 0%

          # =========================================================
          # REPOSITORIES
          # =========================================================
          plugin_repositories: yes

          plugin_repositories_featured: |
            soo6ya/AI-Network-Anomaly-Detection
            soo6ya/tg-converter-bot
            soo6ya/internet-speed-test

          plugin_repositories_pinned: 0
          plugin_repositories_starred: 0
          plugin_repositories_random: 0

          # =========================================================
          # LINES OF CODE
          # =========================================================
          plugin_lines: yes
          plugin_lines_sections: base, repositories

          # =========================================================
          # ACHIEVEMENTS
          # =========================================================
          plugin_achievements: yes
          plugin_achievements_threshold: C

          # =========================================================
          # STARRED TOPICS
          # =========================================================
          plugin_topics: yes
          plugin_topics_sort: stars
          plugin_topics_limit: 15

          # =========================================================
          # RECENTLY STARRED REPOSITORIES
          # =========================================================
          plugin_stars: yes
          plugin_stars_limit: 5

          # =========================================================
          # FOLLOW-UP STATISTICS
          # =========================================================
          plugin_followup: yes

          # =========================================================
          # DISABLED PLUGINS
          # =========================================================

          # Disabled because it previously caused token-scope errors
          plugin_projects: no

          # Disabled because it previously returned an unexpected error
          plugin_habits: no

          # =========================================================
          # OUTPUT
          # =========================================================
          filename: github-metrics.svg
